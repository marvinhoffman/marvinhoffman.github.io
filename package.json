{
  "name": "marvin-posts-generator",
  "version": "1.0.0",
  "description": "Generate posts.json from markdown files with frontmatter",
  "main": "scripts/generate-posts-json.js",
  "scripts": {
    "generate:posts": "node scripts/generate-posts-json.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}